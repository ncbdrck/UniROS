[pytest]
# Make `import uniros` resolve to ./src/uniros when running pytest from
# the package root, so developers can run `pytest` without needing to
# pip install -e . first (and without setting PYTHONPATH by hand).
pythonpath = src

testpaths = tests
addopts = -ra
